import 'cypress-shadow-dom';

describe('Form Stepper: Step of -1 does not show the first page on page load', () => {
/*
  it('should display the first page content on initial load', () => {
    // Visit the test page
    cy.visit('/bug2033');

    // Wait for the page to fully load
    cy.wait(1000);
    cy.log('This test is failed intentionally, a new bug is reported: https://github.com/GovAlta/ui-components/issues/2033');
    // Assert that the first page content is visible
    cy.contains('Page 1 content with status: incomplete').should('be.visible');

  });
*/
});
