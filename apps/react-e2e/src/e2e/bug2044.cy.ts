import 'cypress-shadow-dom';

describe('Feature 2044 Testing', () => {
/*
    it('datepicker is disabled', () => {
        cy.visit('/bug2044');
        cy.wait(1000);
        cy.get('goa-date-picker').shadow().find('goa-popover').find('goa-input').shadow().find('input').should('be.disabled');
    });
*/
});
